import { FiterPipePipe } from './fiter-pipe.pipe';

describe('FiterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
