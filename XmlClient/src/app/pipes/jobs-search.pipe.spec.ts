import { JobsSearchPipe } from './jobs-search.pipe';

describe('JobsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new JobsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
