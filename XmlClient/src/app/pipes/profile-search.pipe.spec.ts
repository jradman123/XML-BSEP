import { ProfileSearchPipe } from './profile-search.pipe';

describe('ProfileSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
